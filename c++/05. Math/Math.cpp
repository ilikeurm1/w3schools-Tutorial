#include <iostream>
#include <cmath>

using namespace std;

int main() {
    cout << max(5, 10) << endl; // 10       (maximum value)
    cout << min(5, 10) << endl; // 5        (minimum value)
    cout << sqrt(64) << endl;   // 8        (square root)
    cout << round(2.6) << endl; // 3        (rounds to the nearest integer)
    cout << log(2);             // 0.693147 (natural logarithm)
    /*
    For more math functions, visit:
    https://www.w3schools.com/cpp/cpp_ref_math.asp // Full C++ Reference
    */
    return 0;
}
